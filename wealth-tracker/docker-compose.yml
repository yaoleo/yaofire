version: '3.8'

services:
  backend:
    build: .
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=development
      - PORT=3001
    volumes:
      - ./server:/app/server
    command: npm start

  # Optional: Add database service later (PostgreSQL, MongoDB, etc.)
  # database:
  #   image: postgres:15-alpine
  #   environment:
  #     POSTGRES_PASSWORD: password
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #
  # volumes:
  #   postgres_data:
